Machine learning application was applied to cifar10 dataset.

The images in the dataset and their classes were taught to the machine.

with GUI,It tells us the classes of the images that the user gives.